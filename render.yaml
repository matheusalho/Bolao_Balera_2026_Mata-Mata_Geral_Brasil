# Render Blueprint — Ranking (Geral) · Jogos do Brasil (estático, sem build)
# Render ▸ New ▸ Blueprint ▸ conectar este repositório ▸ Apply.
services:
  - type: web
    name: bolao-balera-geral
    runtime: static
    buildCommand: "echo site estatico sem build"
    staticPublishPath: .
